node_modules/
dist/
*.log
.DS_Store
# Anchored to the repo root so it ignores only the top-level nflverse snapshot dir,
# NOT source dirs like src/data/.
/data/
*.sqlite
*.sqlite-*
.env
.env.local

# local working docs / session tooling — not part of the shipped repo
.claude/
docs/
